@AGENTS.md

## Notas específicas para Claude Code

- Backend corre en local (ver backend/CLAUDE.md) — configurar URL base de la API
  según entorno: emulador Android → 10.0.2.2, iOS → localhost, dispositivo físico → IP LAN
- Usar Plan Mode (Shift+Tab) antes de tocar pantallas de flujo crítico (botón de pánico, auth)
